# 🎤 Реализация голосового общения с AI через OpenAI TTS

## 📋 Обзор

Этот документ описывает полную реализацию голосового общения между пользователем и AI учителем с использованием OpenAI TTS (Text-to-Speech) и Web Speech API для распознавания речи. Система обеспечивает непрерывное двустороннее голосовое взаимодействие без необходимости использования изображений.

## 🏗️ Архитектура системы

### Основные компоненты

#### 1. **Speech Recognition (Веб Speech API)**
- Распознавание речи пользователя в реальном времени
- Поддержка русского языка (`ru-RU`)
- Непрерывное прослушивание с промежуточными результатами

#### 2. **OpenAI Chat Completions**
- Использование модели `gpt-3.5-turbo` для быстрого ответа
- Системный промпт с инструкциями для образовательного контента
- Конвертация чисел, дат и математических выражений в слова

#### 3. **OpenAI TTS (Text-to-Speech)**
- Параллельная генерация речи для предложений
- Прерывание воспроизведения при новом голосовом вводе
- Конвертация чисел в слова для естественного звучания

#### 4. **Audio Feedback System**
- Звуковые сигналы во время генерации LLM и TTS
- Различные паттерны для разных состояний системы

## 🎯 Ключевые возможности

### 1. **Непрерывное голосовое общение**
- Пользователь может говорить естественным образом
- Система автоматически переключается между прослушиванием и ответом
- Возможность прерывания AI во время ответа

### 2. **Интеллектуальная обработка текста**
- Конвертация чисел: "400" → "четыреста"
- Даты: "12 апреля 1961 года" → "двенадцатого апреля тысяча девятьсот шестьдесят первого года"
- Математика: "2 + 3 = 5" → "два плюс три равно пяти"

### 3. **Параллельная обработка TTS**
- Разделение ответа AI на предложения
- Одновременная генерация аудио для всех предложений
- Последовательное воспроизведение с паузами

## 📁 Структура файлов

```
src/
├── lib/
│   ├── numbersToWords.ts      # Конвертация чисел в слова
│   └── openaiTTS.ts          # OpenAI TTS интеграция
└── pages/
    └── Chat.tsx              # Основной компонент чата
```

## 🔧 Детальная реализация

### 1. Инициализация Speech Recognition

```typescript
// Инициализация Web Speech API
useEffect(() => {
  const SpeechRecognition = window.SpeechRecognition ||
                           window.webkitSpeechRecognition ||
                           window.mozSpeechRecognition ||
                           window.msSpeechRecognition;

  if (SpeechRecognition) {
    recognitionRef.current = new SpeechRecognition();
    recognitionRef.current.continuous = true;
    recognitionRef.current.interimResults = true;
    recognitionRef.current.lang = 'ru-RU';
  }
}, []);
```

### 2. Обработка голосового ввода

```typescript
recognitionRef.current.onresult = async (event) => {
  // Обработка финального результата
  if (finalTranscript.trim() && !isProcessing) {
    isProcessing = true;

    // Прерывание текущего TTS
    if (currentAudioRef.current || isGeneratingTTS) {
      OpenAITTS.stop();
      ttsContinueRef.current = false;
    }

    // Отправка запроса к OpenAI
    const response = await fetch('https://api.openai.com/v1/chat/completions', {
      // ... конфигурация запроса
    });

    // Генерация и воспроизведение ответа
    await speakTextBySentences(assistantMessage.content, assistantMessage.id);
  }
};
```

### 3. Параллельная генерация TTS

```typescript
const speakTextBySentences = async (text: string, messageId: string) => {
  // Разделение текста на предложения
  const sentences = splitIntoSentences(text);

  // Параллельная генерация аудио
  const generationPromises = sentences.map(async (sentence, index) => {
    const audioBuffer = await OpenAITTS.generateSpeech(sentence, {
      voice: 'alloy',
      speed: 0.9,
      model: 'tts-1'
    });
    return { audioBuffer, sentence, index };
  });

  // Ожидание завершения генерации
  const audioResults = await Promise.allSettled(generationPromises);

  // Последовательное воспроизведение
  for (const result of successfulResults) {
    await playSentence(result.audioBuffer, result.sentence, i + 1, successfulResults.length);
  }
};
```

### 4. Конвертация чисел в слова

#### Основные функции в `numbersToWords.ts`:

```typescript
// Основная функция конвертации
export function numberToWords(num: number): string {
  // Обработка миллионов, тысяч, сотен, десятков, единиц
  // Возвращает текстовое представление числа
}

// Конвертация дат
function processDateForTTS(text: string): string {
  const dateRegex = /(\d{1,2})\s+(января|февраля|...)\s+(\d{4})\s+(года?|г\.?)/gi;
  return text.replace(dateRegex, (match, day, month, year, suffix) => {
    const dayWords = getOrdinalForm(dayNum);
    const monthWords = months[month.toLowerCase()];
    const yearWords = getYearOrdinalForm(yearNum);
    return `${dayWords} ${monthWords} ${yearWords} ${suffix}`;
  });
}

// Основная функция обработки текста
export function replaceNumbersInText(text: string): string {
  let processedText = text;
  processedText = processDateForTTS(processedText);
  processedText = processYearsForTTS(processedText);

  // Конвертация оставшихся чисел
  const numberRegex = /\b\d+\b/g;
  processedText = processedText.replace(numberRegex, (match) => {
    const num = parseInt(match, 10);
    return numberToWords(num);
  });

  return processedText;
}
```

### 5. Audio Feedback System

```typescript
// Звуковые сигналы во время генерации
const startContinuousSound = (frequency: number = 600, interval: number = 800) => {
  soundIntervalRef.current = setInterval(() => {
    playBeep(frequency, 100, 'sine');
  }, interval);
};

// Разные паттерны для разных состояний
// LLM генерация: startContinuousSound(500, 1800)
// TTS генерация: startContinuousSound(500, 1800)
```

## 🔄 Поток работы системы

### 1. **Инициализация**
- Настройка Speech Recognition
- Проверка доступности OpenAI API
- Инициализация аудио контекста

### 2. **Прослушивание**
- Непрерывное распознавание речи
- Обработка промежуточных результатов
- Прерывание TTS при обнаружении речи

### 3. **Обработка запроса**
- Отправка текста к OpenAI Chat Completions
- Получение ответа AI
- Конвертация чисел в слова

### 4. **Генерация речи**
- Разделение ответа на предложения
- Параллельная генерация аудио файлов
- Последовательное воспроизведение

### 5. **Обратная связь**
- Звуковые сигналы во время обработки
- Визуальные индикаторы состояния
- Возможность прерывания

## ⚙️ Конфигурация

### Переменные окружения

```env
VITE_OPENAI_API_KEY=your_openai_api_key_here  # OpenAI API ключ
```

### Параметры TTS

```typescript
const ttsOptions = {
  voice: 'alloy',     // Голос: alloy, echo, fable, onyx, nova, shimmer
  speed: 0.9,         // Скорость речи: 0.25 - 4.0
  model: 'tts-1'      // Модель: tts-1 или tts-1-hd
};
```

## 🛠️ Обработка ошибок

### Speech Recognition ошибки

```typescript
recognitionRef.current.onerror = (event) => {
  switch (event.error) {
    case 'not-allowed':
      alert('Разрешите доступ к микрофону');
      break;
    case 'no-speech':
      console.warn('Речь не обнаружена');
      break;
    case 'network':
      alert('Ошибка сети при распознавании речи');
      break;
  }
};
```

### OpenAI API ошибки

```typescript
if (!response.ok) {
  const errorData = await response.json();

  switch (response.status) {
    case 401:
      throw new Error('Неверный API ключ OpenAI');
    case 429:
      throw new Error('Превышен лимит запросов');
    case 500:
      throw new Error('Ошибка сервера OpenAI');
  }
}
```

## 📊 Производительность

### Оптимизации

1. **Параллельная генерация TTS** - одновременная обработка всех предложений
2. **Кэширование аудио** - повторное использование сгенерированных файлов
3. **Асинхронная обработка** - неблокирующий UI
4. **Memory management** - очистка ресурсов после использования

### Метрики производительности

- **Время ответа AI**: ~2-5 секунд
- **Генерация TTS**: ~0.5-1 секунда на предложение
- **Общее время**: ~3-8 секунд на полный цикл

## 🔐 Безопасность

### API ключи
- Хранение в переменных окружения
- Не логирование в коде
- Валидация перед использованием

### Данные пользователя
- Локальная обработка речи
- Шифрование при передаче
- Очистка временных данных

## 🧪 Тестирование

### Ручное тестирование

```typescript
// Тест конвертации чисел
console.log(numberToWords(1961)); // "один тысяча девятьсот шестьдесят один"
console.log(replaceNumbersInText("12 апреля 1961 года"));
// "двенадцатого апреля тысяча девятьсот шестьдесят первого года"
```

### Интеграционное тестирование

1. Проверка Speech Recognition в разных браузерах
2. Тестирование прерывания TTS
3. Валидация обработки ошибок
4. Проверка производительности

## 🚀 Развертывание

### Production готовность

1. **Environment variables** - настройка API ключей
2. **Build optimization** - минификация и оптимизация
3. **Error boundaries** - обработка ошибок в продакшене
4. **Monitoring** - логирование и метрики

### Браузерная поддержка

- ✅ Chrome/Edge (полная поддержка)
- ✅ Firefox (ограниченная поддержка)
- ⚠️ Safari (требует разрешений)
- ❌ IE (не поддерживается)

## 📚 API Reference

### OpenAI TTS класс

```typescript
class OpenAITTS {
  static generateSpeech(text: string, options?: TTSOptions): Promise<ArrayBuffer>
  static speakText(text: string, options?: TTSOptions): Promise<void>
  static stop(): void
  static isPlaying(): boolean
}
```

### Speech Recognition интерфейсы

```typescript
interface SpeechRecognitionEvent extends Event {
  readonly resultIndex: number;
  readonly results: SpeechRecognitionResultList;
}

interface SpeechRecognition extends EventTarget {
  continuous: boolean;
  interimResults: boolean;
  lang: string;
  start(): void;
  stop(): void;
  abort(): void;
}
```

## 🔄 Будущие улучшения

### Планируемые возможности

1. **Многоязычная поддержка** - английский, испанский и т.д.
2. **Эмоциональные голоса** - разные тона для разных типов контента
3. **Offline режим** - локальная обработка без интернета
4. **Голосовые команды** - управление системой голосом
5. **Адаптивная скорость речи** - автоматическая настройка темпа

---

## 📞 Контакты

Для вопросов по реализации обращайтесь к разработчику проекта.

**GitHub:** https://github.com/RockInMyHead/teacher_windexs
