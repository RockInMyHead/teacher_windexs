# ✅ Чеклист развёртывания голосового общения

## 📋 До развёртывания

### 1. Проверьте наличие файлов

```bash
# Основной модуль
✅ ls src/lib/voiceComm.ts
✅ ls src/lib/openaiTTS.ts

# React интеграция
✅ ls src/hooks/useVoiceComm.ts

# Компонент урока
✅ ls src/components/InteractiveLessonChat.tsx

# Примеры
✅ ls src/examples/VoiceCommExamples.tsx

# Документация
✅ ls docs/VOICE_COMMUNICATION_GUIDE.md
✅ ls VOICE_COMMUNICATION_README.md
✅ ls VOICE_INTEGRATION_QUICK_START.md
```

### 2. Проверьте зависимости

```json
// package.json должен содержать:
{
  "dependencies": {
    "react": "^18.x",
    "lucide-react": "latest",  // для иконок Mic, MicOff, Volume2, etc.
    "@radix-ui/...": "...",     // UI компоненты
    "typescript": "^5.x"
  }
}
```

Все зависимости уже установлены в проекте ✅

### 3. Проверьте переменные окружения

```bash
# .env или .env.local
VITE_OPENAI_API_KEY=sk-...  # Обязателен!
```

### 4. Проверьте TypeScript конфигурацию

```json
// tsconfig.json
{
  "compilerOptions": {
    "target": "ES2020",
    "jsx": "react-jsx",
    "strict": true,
    "esModuleInterop": true
  }
}
```

## 🔍 Проверка функционала

### Локальное тестирование

```bash
# Запустить dev сервер
npm run dev

# Открыть браузер
# http://localhost:5173

# Проверить консоль на ошибки (F12)
# Должны появиться логи инициализации
```

### Тестовые сценарии

#### 1. TTS Тест
```
1. Открыть интерактивный урок
2. Ожидать автоматического озвучивания
3. Услышать первый экран урока
4. Проверить натуральное произношение
5. Посмотреть логи в консоли (F12)
```

#### 2. STT Тест
```
1. Нажать кнопку 🎤 (микрофона)
2. Сказать: "Привет, как дела?"
3. Ожидать распознавания в поле ввода
4. Проверить, что интеримный текст показывался
5. Убедиться, что финальный текст распознан правильно
```

#### 3. Совместный тест
```
1. Получить ответ от ассистента
2. Нажать 🔊 для озвучивания
3. Услышать ответ
4. Попытаться нажать 🎤 (должен быть заблокирован)
5. Дождаться конца озвучивания
6. Нажать 🎤 для записи ответа
```

#### 4. Браузер совместимости
```
✅ Chrome 71+
✅ Firefox 95+
✅ Safari 14.1+
✅ Edge 79+
```

## 🚀 Развёртывание

### Шаг 1: Сборка проекта

```bash
# Очистить старую сборку
rm -rf dist/

# Собрать проект
npm run build

# Проверить результат
ls -la dist/
```

### Шаг 2: Проверка сборки

```bash
# Запустить preview
npm run preview

# Открыть http://localhost:4173
# Проверить, что всё работает
```

### Шаг 3: Проверить размер бандла

```bash
# Размер не должен значительно увеличиться
# VoiceComm добавляет ~15KB
```

### Шаг 4: Развернуть на сервер

```bash
# Загрузить dist/ на сервер
scp -r dist/ user@server:/var/www/app/

# Или используйте ваш способ развёртывания
```

### Шаг 5: Проверить на сервере

```bash
# 1. Открыть HTTPS (важно!)
# https://example.com

# 2. Проверить консоль браузера (F12)
# Должны быть логи голосового общения

# 3. Проверить микрофон
# Должен запросить разрешение

# 4. Проверить озвучивание
# Должны услышать голос
```

## 🔐 Безопасность

### HTTPS (обязателен!)
```
⚠️  Микрофон работает ТОЛЬКО на HTTPS!
✅  http://localhost/ - OK (локальная разработка)
✅  https://example.com/ - OK (продакшн)
❌  http://example.com/ - НЕ будет работать
```

### API ключ
```
⚠️  Не коммитьте VITE_OPENAI_API_KEY в git!
✅  Используйте .env.local (в .gitignore)
✅  Установите переменную на сервере
```

### CORS
```
✅  OpenAI API должен быть доступен
✅  Проксируйте через ваш backend если нужно
```

## 🛠️ Отладка на сервере

### Проверить логи

```bash
# Открыть DevTools (F12)
# Вкладка Console
# Фильтр: VoiceComm

# Должны быть логи:
# 🎤 Initializing...
# ✅ Voice Communication initialized
# 🎙️ Listening started
```

### Проверить ошибки

```bash
# Вкладка Network (F12)
# Фильтр: audio или speech

# Должны быть запросы к:
# POST /api/audio/speech (TTS)
```

### Проверить разрешения

```javascript
// В консоли браузера:
navigator.mediaDevices.getUserMedia({audio: true})
  .then(() => console.log('✅ Микрофон доступен'))
  .catch(err => console.error('❌ Ошибка:', err.message))
```

## 📊 Мониторинг

### Метрики для отслеживания

```
- Процент пользователей с поддержкой голоса
- Успешно распознанные фразы (STT)
- Средняя длительность озвучивания (TTS)
- Ошибки микрофона
- Ошибки API OpenAI
```

### Логирование

```typescript
// Добавьте в аналитику
analytics.track('voice_recognition_success', {
  text: recognizedText,
  confidence: confidence
});

analytics.track('tts_error', {
  error: errorMessage,
  text: attemptedText
});
```

## 🚨 Потенциальные проблемы

### Проблема 1: HTTPS не настроена
```
❌ Микрофон не работает на HTTP

Решение:
✅ Настроить SSL сертификат
✅ Перенаправлять HTTP на HTTPS
```

### Проблема 2: API ключ не установлен
```
❌ TTS не работает - "API key not found"

Решение:
✅ Проверить VITE_OPENAI_API_KEY в .env
✅ Перезагрузить сервер
```

### Проблема 3: CORS ошибки
```
❌ "Access-Control-Allow-Origin"

Решение:
✅ Проксировать API через backend
✅ Проверить CORS на OpenAI API
```

### Проблема 4: Микрофон не работает на мобильных
```
❌ "Microphone not available"

Решение:
✅ Проверить, что сайт на HTTPS
✅ Попросить разрешение у пользователя
✅ Тестировать на реальном устройстве
```

## ✅ Финальный чеклист

```
Перед продакшном убедитесь:

□ Все файлы на месте
□ Нет TypeScript ошибок (npm run build успешен)
□ HTTPS настроена
□ VITE_OPENAI_API_KEY установлен
□ Тестировали TTS и STT
□ Тестировали на разных браузерах
□ Микрофон работает с разрешениями
□ API call логируется для отладки
□ Ошибки обрабатываются корректно
□ Производительность в норме (<100ms задержка)
□ Документация доступна пользователям
□ Резервный план если голос не работает
```

## 📞 Контакты для поддержки

При возникновении проблем:

1. **Смотрите логи:** F12 → Console
2. **Читайте документацию:** `docs/VOICE_COMMUNICATION_GUIDE.md`
3. **Проверьте примеры:** `src/examples/VoiceCommExamples.tsx`
4. **Создайте Issue** с логами и описанием

## 🎓 Обучение команды

### Для разработчиков

1. Читайте `docs/VOICE_COMMUNICATION_GUIDE.md`
2. Смотрите примеры в `src/examples/`
3. Изучите `src/lib/voiceComm.ts`
4. Посмотрите интеграцию в `InteractiveLessonChat.tsx`

### Для тестировщиков

1. Читайте `VOICE_COMMUNICATION_README.md`
2. Используйте `VOICE_INTEGRATION_QUICK_START.md`
3. Тестируйте с разными браузерами
4. Проверьте примеры работают

## 📈 Метрики успеха

```
✅ 95%+ успешного распознавания речи (на русском)
✅ <500ms задержки между речью и текстом
✅ Естественное озвучивание без пауз
✅ <1% ошибок API
✅ 99%+ доступность сервиса
```

## 🎉 Готово!

Голосовое общение готово к развёртыванию:

```bash
# 1. Финальная сборка
npm run build

# 2. Тестирование
npm run preview

# 3. Развёртывание
# Загрузите dist/ на сервер

# 4. Проверка
# Откройте https://example.com и тестируйте
```

**Поздравляем! 🎓📚🎤**

Ваша система голосового обучения готова к использованию!

