# Переменные окружения для Docker развертывания

# OpenAI API Key - ОБЯЗАТЕЛЬНО!
OPENAI_API_KEY=your_openai_api_key_here

# Порт сервера (внутри контейнера)
PORT=1031

# Окружение
NODE_ENV=production

# База данных (автоматически устанавливается в Dockerfile)
DATABASE_PATH=/app/data/teacher_platform.db

# Для запуска в Docker:
# 1. Создайте файл .env в корне проекта
# 2. Скопируйте содержимое из DOCKER_ENV_EXAMPLE.txt
# 3. Установите ваш OPENAI_API_KEY
# 4. Запустите: docker-compose up --build
