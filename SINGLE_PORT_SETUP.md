# 🚀 Single Port Setup - Всё на порт 1031

## 🎯 **Проблема:**
У тебя только один открытый порт 1031, а нужно запустить:
- Frontend (React/Vite)
- API Proxy (для OpenAI)

## ✅ **Решение:**
Один сервер на порту 1031, который:
- Обрабатывает API запросы (`/api/*`)
- Проксирует frontend запросы на внутренний Vite сервер (1032)

## 🏗️ **Архитектура:**

```
Browser (localhost:1031)
    ↓
Single Port Server (1031)
    ├─ /api/* → OpenAI API через proxy
    └─ /* → Frontend Server (1032)
```

## 🚀 **Как запустить:**

### Вариант 1 - Простой запуск:
```bash
npm run start:single-port
```

### Вариант 2 - Ручной запуск:
```bash
# В одном терминале - frontend
NODE_ENV=production VITE_DEV_PORT=1032 npm run dev

# В другом терминале - proxy
PORT=1031 node proxy-server.cjs
```

## 📊 **Что происходит:**

1. **Frontend сервер** запускается на порту 1032 (внутренний)
2. **Proxy сервер** запускается на порту 1031
3. **Proxy сервер** проксирует не-API запросы на frontend (1032)
4. **Всё доступно** через порт 1031

## 🌐 **Доступ:**

- **Главная страница:** `http://localhost:1031/`
- **API endpoints:** `http://localhost:1031/api/*`
- **Health check:** `http://localhost:1031/health`

## 🛑 **Остановка:**

```bash
# Ctrl+C в терминале где запущен single-port-server.js
# Или найди процессы:
ps aux | grep node

# И убей:
kill -9 <PID>
```

## 🔧 **Технические детали:**

### single-port-server.js:
- Запускает frontend на 1032
- Запускает proxy на 1031
- Управляет жизненным циклом обоих процессов

### proxy-server.cjs:
- Слушает порт 1031
- API запросы (`/api/*`) → OpenAI
- Остальные запросы → Frontend (1032)

### vite.config.ts:
- Frontend порт: 1032 (внутренний)
- Proxy API: через основной порт 1031

## ✅ **Проверка работы:**

```bash
# Проверь что сервер слушает
curl http://localhost:1031/

# Проверь API
curl http://localhost:1031/api/health

# Проверь что оба процесса работают
ps aux | grep node | grep -E "(1031|1032)"
```

## 🎯 **Для production:**

```bash
# Создай systemd сервис
sudo tee /etc/systemd/system/teacher-single.service > /dev/null <<EOF
[Unit]
Description=Windexs Teacher Single Port Server
After=network.target

[Service]
Type=simple
User=$USER
WorkingDirectory=$(pwd)
Environment="PATH=/usr/bin:/bin"
ExecStart=/usr/bin/npm run start:single-port
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

# Включи и запусти
sudo systemctl daemon-reload
sudo systemctl enable teacher-single
sudo systemctl start teacher-single
```

## 📋 **Файлы изменены:**

1. **`proxy-server.cjs`** - добавлен frontend proxy middleware
2. **`vite.config.ts`** - frontend порт изменен на 1032
3. **`package.json`** - добавлен скрипт `start:single-port`
4. **`single-port-server.js`** - новый скрипт для запуска всего

## 💡 **Преимущества:**

- ✅ **Один порт** для всего приложения
- ✅ **Простая настройка** для ограниченных сред
- ✅ **Работает** с существующими API вызовами
- ✅ **Легко развернуть** в production

## 🚨 **Важно для джуна:**

Это **упрощённая архитектура** для случаев, когда нет возможности открыть много портов. В production лучше держать сервисы отдельно для лучшей масштабируемости и отладки.

**Используй это решение только если у тебя действительно ограничения по портам!** 🎯
