# ❌ Ошибка сборки Frontend

## 🚨 Проблема

```
❌ Ошибка сборки frontend
```

Процесс `npm run build` падает при запуске сервера.

## ✅ Быстрое Решение

### На СЕРВЕРЕ выполните:

```bash
# 1. Остановить приложение
pm2 stop teacher-app

# 2. Перейти в проект
cd ~/windexs-ai-learn

# 3. Очистить всё
rm -rf dist node_modules .vite
npm cache clean --force

# 4. Переустановить зависимости
npm install

# 5. Собрать с выводом ошибок
npm run build 2>&1 | tee build.log

# 6. Если ошибка - пришлите вывод:
cat build.log

# 7. Если всё OK - запустить сервер
pm2 start single-port-server.cjs --name="teacher-app"
```

## 🔍 Диагностика

### Проверить что есть:

```bash
# node_modules?
ls node_modules | head -5

# dist папка?
ls dist | head -5

# vite.config.ts?
ls vite.config.ts

# Посмотреть ошибку сборки
npm run build
```

## 🛠️ Типичные Причины

### 1. node_modules поломаны

```bash
rm -rf node_modules package-lock.json
npm install
npm run build
```

### 2. Конфликт версий

```bash
npm cache clean --force
npm install
npm run build
```

### 3. Старая сборка мешает

```bash
rm -rf dist .vite build
npm run build
```

### 4. Ошибка в коде

```bash
npm run build 2>&1 | head -50
# Пришлите первые 50 строк ошибки
```

## 💡 Альтернативное Решение

### Собрать локально (на вашем компьютере)

```bash
# Локально на вашем компьютере:
npm run build

# Проверить что есть dist
ls -la dist

# Залить на GitHub
git add dist/
git commit -m "Add built dist folder"
git push
```

### На сервере (просто запустить)

```bash
# На сервере:
cd ~/windexs-ai-learn
git pull

# Запустить (БЕЗ сборки, т.к. dist уже есть)
pm2 start single-port-server.cjs --name="teacher-app"
```

## 📝 Полная Пошаговая Инструкция

### Шаг 1: SSH на сервер

```bash
ssh user@teacher.windexs.ru
```

### Шаг 2: Остановить текущий процесс

```bash
pm2 stop teacher-app
# или Ctrl+C если запущен вручную
```

### Шаг 3: Перейти в проект

```bash
cd ~/windexs-ai-learn
```

### Шаг 4: Очистить кэш

```bash
rm -rf dist
rm -rf node_modules
rm -rf .vite
npm cache clean --force
```

### Шаг 5: Переустановить зависимости

```bash
npm install
```

### Шаг 6: Собрать с логом

```bash
npm run build 2>&1 | tee build.log
```

### Шаг 7: Если Ошибка - Пришлите Вывод

```bash
cat build.log
```

### Шаг 8: Если Всё OK - Запустить

```bash
pm2 start single-port-server.cjs --name="teacher-app"
```

### Шаг 9: Проверить Статус

```bash
pm2 status
# Должен быть "online"

pm2 logs
# Должны видеть логи приложения
```

## ✨ Когда Всё Работает

Должны увидеть:

```
teacher-app | ✅ Frontend собран
teacher-app | 🚀 Запуск единого сервера на порту 1031...
teacher-app | ✅ SQLite Database connected
```

## 🎯 Если Всё Ещё Не Работает

### Нужна информация:

1. Полный вывод ошибки `npm run build`
2. Версия Node.js: `node --version`
3. Версия npm: `npm --version`
4. Содержимое `.env` файла (без чувствительных данных)
5. Вывод `cat package.json | grep -A 5 "scripts"`

### Получить все логи:

```bash
# Сохранить логи в файл
npm run build > build.log 2>&1
cat build.log > /tmp/build_error.txt

# Или просто скопировать вывод из консоли
```

## 🔧 Проверить Окружение

```bash
# Версия Node
node --version

# Версия npm
npm --version

# Список глобально установленных пакетов
npm list -g --depth=0

# Проверить .env файл
cat .env
```

## 📊 Если Проблема Специфичная

### Cannot find module

```bash
npm install
npm run build
```

### Permission denied

```bash
sudo chown -R $USER:$USER node_modules
npm install
```

### Out of memory

```bash
# Увеличить лимит памяти для Node
node --max-old-space-size=4096 single-port-server.cjs
```

### EACCES: permission denied

```bash
# Исправить права доступа
sudo chmod -R 755 ~/windexs-ai-learn
cd ~/windexs-ai-learn
npm install
npm run build
```

---

**Версия:** 1.0.0  
**Дата:** Ноябрь 2025

**Главное: пришлите ПОЛНЫЙ вывод `npm run build` чтобы увидеть реальную ошибку!**

