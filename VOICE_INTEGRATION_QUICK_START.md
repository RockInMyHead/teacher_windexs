# ⚡ Быстрая интеграция голосового общения

## 📋 Что было сделано?

### ✅ Новые файлы

1. **`src/lib/voiceComm.ts`** (500+ строк)
   - Полный модуль для TTS/STT
   - Управление микрофоном и воспроизведением
   - Обработка ошибок

2. **`src/hooks/useVoiceComm.ts`** (200+ строк)
   - React хук для удобного использования VoiceComm
   - Управление состоянием
   - Коллбеки для интеграции

3. **`src/examples/VoiceCommExamples.tsx`** (500+ строк)
   - 5 полностью работающих примеров
   - От базовых до продвинутых
   - Демонстрация всех функций

4. **`docs/VOICE_COMMUNICATION_GUIDE.md`** (500+ строк)
   - Полная документация
   - API Reference
   - Решение проблем

5. **`VOICE_COMMUNICATION_README.md`** (400+ строк)
   - Краткое руководство пользователя
   - Примеры использования
   - Часто задаваемые вопросы

### ✅ Обновлённые файлы

1. **`src/components/InteractiveLessonChat.tsx`**
   - Интеграция VoiceComm
   - Кнопка микрофона для записи
   - Автоматическое озвучивание контента
   - Визуальные индикаторы записи
   - Улучшенная UX

## 🚀 Как использовать?

### 1️⃣ В интерактивном уроке (УЖЕ ГОТОВО)

Компонент `InteractiveLessonChat` уже настроен:

```tsx
<InteractiveLessonChat
  lessonTitle="Английский"
  lessonTopic="Present Continuous"
  lessonAspects="..."
  lessonContent={lessonData}
  onComplete={() => navigate('/courses')}
/>
```

**Что автоматически работает:**
- ✅ Озвучивание первого экрана урока
- ✅ Кнопка микрофона для записи ответов
- ✅ Кнопка громкости для озвучивания
- ✅ Визуальные индикаторы

### 2️⃣ В своём компоненте (Хук)

```tsx
import { useVoiceComm } from '@/hooks/useVoiceComm';

function MyComponent() {
  const { isListening, speak, startListening } = useVoiceComm();

  return (
    <>
      <button onClick={startListening}>🎤 Запись</button>
      <button onClick={() => speak('Привет!')}>🔊 Озвучить</button>
    </>
  );
}
```

### 3️⃣ Прямое использование (API)

```tsx
import { VoiceComm } from '@/lib/voiceComm';

// Инициализация
useEffect(() => {
  VoiceComm.init({ language: 'ru-RU' }, {
    onTranscript: (text) => console.log(text)
  });
  return () => VoiceComm.cleanup();
}, []);

// Использование
<button onClick={() => VoiceComm.startListening()}>Запись</button>
<button onClick={() => VoiceComm.speakText('Текст')}>Озвучить</button>
```

## 🎯 Основные функции

### TTS (Text-to-Speech)

```typescript
// Озвучить текст
await VoiceComm.speakText('Привет, ученик!', {
  language: 'ru-RU',
  ttsVoice: 'nova',  // Женский голос
  ttsSpeed: 1.0      // Нормальная скорость
});

// Остановить озвучивание
VoiceComm.stopSpeaking();
```

**Доступные голоса:**
- `nova` - Женский, естественный (по умолчанию)
- `shimmer` - Женский, бодрый
- `alloy` - Мужской, нейтральный
- `echo` - Мужской, глубокий
- `fable` - Мужской, дружелюбный
- `onyx` - Мужской, убедительный

### STT (Speech-to-Text)

```typescript
// Начать запись
VoiceComm.startListening();

// Остановить запись
VoiceComm.stopListening();

// Отменить запись
VoiceComm.abortListening();

// Получить результат в коллбэке onTranscript
```

**Коллбэки:**
```typescript
{
  onListeningStart: () => {}, // Запись началась
  onListeningEnd: () => {},   // Запись закончилась
  onTranscript: (text, isFinal) => {}, // Результат
  onError: (error) => {},     // Ошибка
  onPlayStart: () => {},      // TTS началась
  onPlayEnd: () => {}         // TTS закончилась
}
```

## 🎨 UI/UX

### Кнопка микрофона
```
┌─────────────────────┐
│ 🎤 Начать запись   │ ← Обычное состояние
└─────────────────────┘
     ↓ (клик)
┌─────────────────────┐
│ 🎙️ Слушаю...      │ ← Во время записи (красная)
└─────────────────────┘
```

### Индикатор записи
```
┌────────────────────────────┐
│ 🎙️ Слушаю вас...           │
│ Интеримный текст... 🔄     │
└────────────────────────────┘
```

### Кнопка громкости
```
┌─────────────────────┐
│ 🔊 Озвучить        │ ← Обычное состояние
└─────────────────────┘
     ↓ (клик)
┌─────────────────────┐
│ 🔊 Читаю...        │ ← Во время воспроизведения (активная)
└─────────────────────┘
```

## 📊 Статистика

| Компонент | Строк кода | Функции |
|-----------|-----------|---------|
| voiceComm.ts | 500+ | 15+ методов |
| useVoiceComm.ts | 200+ | React интеграция |
| InteractiveLessonChat.tsx | +150 | UI + управление |
| Примеры | 500+ | 5 полных примеров |
| Документация | 1000+ | Полная документация |
| **ИТОГО** | **2000+** | **Полная система** |

## 🔧 Требования

### Браузер
- Chrome 71+ ✅
- Firefox 95+ ✅
- Safari 14.1+ ✅
- Edge 79+ ✅

### Окружение
```env
VITE_OPENAI_API_KEY=sk-...
```

### Интернет
- Для TTS (OpenAI API)
- Для STT (Web Speech API требует интернет)

## ✨ Примеры работают?

Да! Смотрите в `src/examples/VoiceCommExamples.tsx`:

1. **BasicVoiceExample** - Базовый пример с кнопками
2. **VoiceChatExample** - Чат с голосовым вводом
3. **VoiceSettingsExample** - Настройка голосов
4. **BrowserSupportExample** - Проверка поддержки
5. **ErrorHandlingExample** - Обработка ошибок

### Импорт примеров

```tsx
import { VoiceCommExamplesPage } from '@/examples/VoiceCommExamples';

// В маршруте
<Route path="/voice-examples" element={<VoiceCommExamplesPage />} />
```

## 🐛 Отладка

### Видеть все логи
1. Откройте F12 (Developer Tools)
2. Переключитесь на вкладку Console
3. Начните использовать голос
4. Смотрите логи в консоли

**Примеры логов:**
```
🎤 Initializing Voice Communication...
✅ Voice Communication initialized
🎙️ Listening started
🔄 Interim transcript: "При..."
✅ Final transcript: "Привет, как дела?"
🎤 Starting sentence-by-sentence TTS...
🎵 Speaking sentence 1/3
✅ Sentence 1 completed
```

### Проверить поддержку

```javascript
// В консоли браузера (F12):
VoiceUtils.checkBrowserSupport()
// { speechRecognition: true, audioAPI: true, mediaDevices: true }
```

## 📱 Мобильные устройства

**Поддержка:**
- iOS 14.5+ (Safari)
- Android 7+ (Chrome)

**Ограничения:**
- HTTPS обязателен
- Микрофон может требовать разрешения

## 🎓 Для обучения

Этот модуль демонстрирует:

✅ TypeScript с интерфейсами  
✅ Web Speech API  
✅ Web Audio API  
✅ React Hooks (useEffect, useState, useCallback)  
✅ Обработка ошибок и Edge Cases  
✅ Асинхронное программирование (async/await)  
✅ State Management  
✅ Интеграция с внешним API (OpenAI)  

## 🚀 Дальнейшее развитие

Возможные улучшения:

- [ ] Оффлайн TTS (Web Speech Synthesis)
- [ ] Запись сессий
- [ ] Анализ речи (эмоции, темп, etc.)
- [ ] Поддержка дополнительных языков
- [ ] Кэширование озвучивания
- [ ] Улучшенный UI с волнами
- [ ] Интеграция с аналитикой

## ❓ Часто задаваемые вопросы

**Q: Почему не работает микрофон?**  
A: 1) Проверьте разрешение браузера, 2) Проверьте HTTPS (на некоторых сервисах), 3) Перезагрузите страницу

**Q: Как изменить голос?**  
A: Передайте параметр при инициализации:
```typescript
VoiceComm.init({ ttsVoice: 'shimmer' }, ...)
```

**Q: Можно ли использовать офлайн?**  
A: Пока нет, требуется интернет для OpenAI API

**Q: Работает на мобильных?**  
A: Да, на iOS 14.5+ и Android 7+ с Chrome/Safari

**Q: Как отключить голос?**  
A: Передайте `enabled: false` при создании хука:
```typescript
useVoiceComm({ enabled: false })
```

## 📚 Дополнительные ресурсы

- **Полная документация:** `docs/VOICE_COMMUNICATION_GUIDE.md`
- **Примеры кода:** `src/examples/VoiceCommExamples.tsx`
- **Компонент урока:** `src/components/InteractiveLessonChat.tsx`
- **Хук React:** `src/hooks/useVoiceComm.ts`
- **Основной модуль:** `src/lib/voiceComm.ts`

## ✅ Что теперь работает?

### В интерактивном уроке:

✅ Автоматическое озвучивание контента  
✅ Запись голоса ученика (микрофон)  
✅ Распознавание речи в реальном времени  
✅ Озвучивание ответов ассистента  
✅ Визуальные индикаторы  
✅ Обработка ошибок  
✅ Совместимость браузеров  

### Новые возможности:

✅ Кнопка 🎤 для записи голоса  
✅ Кнопка 🔊 для озвучивания  
✅ Индикатор "Слушаю вас..."  
✅ Интеримный текст во время записи  
✅ Паузы между предложениями (300ms)  
✅ Разные голоса и скорости  
✅ Русский и английский языки  

## 🎉 Готово!

Голосовое общение полностью интегрировано в систему.

**Начните использовать:**
1. Откройте интерактивный урок
2. Нажмите кнопку 🎤 для записи
3. Говорите на русском/английском
4. Нажмите 🔊 чтобы услышать ответ

**Наслаждайтесь голосовым обучением! 🎓📚**

