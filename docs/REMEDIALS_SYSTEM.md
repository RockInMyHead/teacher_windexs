# 🔄 Система Повторения и Ремедиалов

## Описание

Система автоматически отслеживает ошибки ученика при выполнении практических заданий в уроке и добавляет **ремедиальные блоки** для повторения сложного материала в конце урока.

## 🎯 Как это работает?

### Процесс повторения:

```
Урок (15 блоков)
    ↓
Ученик выполняет практические блоки
    ↓
❌ Некоторые ответы неправильные
    ↓
Система отслеживает ошибки
    ↓
Генерирует РЕМЕДИАЛЬНЫЕ БЛОКИ
    ↓
Добавляет их в конец урока
    ↓
Ученик повторяет сложный материал
    ↓
✅ Закрепляет знания
```

## 📊 Компоненты системы

### 1. `adaptiveLessonFlow.ts` - Основная логика

#### Класс `LessonProgressTracker`

```typescript
// Инициализация
const tracker = new LessonProgressTracker('lesson-1', 15);

// Запись ответа
tracker.recordAnswer(
  blockId: 2,
  blockType: 'practice',
  blockTitle: 'Практика 1: Модальные глаголы',
  topic: 'modals',
  questionId: 'q1',
  question: 'You must do homework',
  userAnswer: 'must', // Ответ ученика
  correctAnswer: 'must', // Правильный ответ
  difficulty: 'beginner'
);

// Получение неправильных блоков
const failedBlocks = tracker.getFailedBlocks();
// Возвращает: [{blockId: 2, title: '...', failureCount: 1, ...}]

// Получение статистики
const stats = tracker.getStatistics();
// { totalAnswers: 10, correctAnswers: 7, accuracy: 70%, failedBlocksCount: 2, ... }
```

#### Функция генерации ремедиалов

```typescript
// Генерирует промпт для AI
const prompt = generateRemedialsPrompt(failedBlocks, 'English Language');

// Возвращает: "Создай ремедиальные блоки для повторения следующих тем: ..."
```

#### Вставка ремедиалов

```typescript
// Исходные 15 блоков + ремедиальные блоки
const fullLesson = insertRemedialsIntoLesson(
  originalBlocks,  // 15 основных блоков
  remedialsBlocks  // Сгенерированные ремедиалы
);

// Структура:
// - Блоки 1-15 (оригинальные)
// - Разделитель "🔄 Повторение трудного материала"
// - Ремедиальные блоки (1000+)
// - Финальный блок "✅ Отлично!"
```

### 2. `RemedialsSystem.tsx` - UI компоненты

#### RemedialsSystemComponent - Главный компонент

```typescript
<RemedialsSystemComponent
  tracker={tracker}
  onRemedialsGenerated={(count) => console.log(`Added ${count} remedials`)}
/>
```

**Отображает:**
- 📊 Статистика прогресса (всего вопросов, точность, к повторению)
- 🔄 Список неправильных блоков с количеством ошибок
- 💡 Инструкции по повторению
- ✅ Статус генерации ремедиалов

#### RemedialsBlock - Компонент ремедиального блока

```typescript
<RemedialsBlock
  block={remedialsBlockData}
  onAnswerSubmit={(questionId, answer, isCorrect) => {
    // Обработка ответа
    console.log(`Question: ${questionId}, Correct: ${isCorrect}`);
  }}
/>
```

## 🔧 Пример использования в Chat.tsx

```typescript
// 1. Создаем трекер в начале урока
const tracker = new LessonProgressTracker('lesson-1', 15);

// 2. Записываем каждый ответ
const isCorrect = tracker.recordAnswer(
  block.id,
  block.type,
  block.title,
  block.topic,
  question.id,
  question.question,
  userAnswer,
  question.correctAnswer,
  block.difficulty
);

// 3. Проверяем, нужны ли ремедиалы
if (tracker.shouldAddRemedials()) {
  // 4. Генерируем ремедиалы через AI
  const failedBlocks = tracker.getFailedBlocks();
  const remedialsPrompt = generateRemedialsPrompt(failedBlocks, courseName);
  
  // Отправляем в OpenAI API...
  const remedialsBlocks = await callOpenAI(remedialsPrompt);
  
  // 5. Регистрируем добавленные ремедиалы
  tracker.recordRemedialsAdded(
    failedBlocks.map(b => b.blockId),
    15 // После последнего оригинального блока
  );
  
  // 6. Вставляем в урок
  const fullLesson = insertRemedialsIntoLesson(originalBlocks, remedialsBlocks);
  
  // 7. Отображаем урок с ремедиалами
  setLessonBlocks(fullLesson);
}

// 8. Показываем компонент системы
<RemedialsSystemComponent tracker={tracker} />
```

## 📈 Структура ремедиального блока

Каждый ремедиальный блок содержит:

```json
{
  "id": "remedial_2_v1",
  "type": "practice",
  "title": "Повторение: Модальные глаголы (Подход #2)",
  "originalBlockId": 2,
  "content": "Давайте разберем модальные глаголы с другой стороны...",
  "instructions": "Попробуй снова с новыми примерами",
  "difficulty": "beginner",
  "isRemedial": true,
  "questions": [
    {
      "id": "q1",
      "question": "Что означает must в контексте правила?",
      "options": [
        "Возможность",
        "Обязательство", // Правильный ответ
        "Разрешение",
        "Рекомендация"
      ],
      "correctAnswer": 1,
      "explanation": "Must используется для выражения обязательства или необходимости"
    }
  ]
}
```

## 📊 Статистика и отчеты

```typescript
// Получение статистики
const stats = tracker.getStatistics();
console.log(`
  Точность: ${stats.accuracy}%
  Блоков для повторения: ${stats.failedBlocksCount}
  Ремедиалов добавлено: ${stats.remedialsAddedCount}
`);

// Генерация полного отчета
const report = generateRemedialsReport(tracker.getProgress());
console.log(report);

/* Вывод:
# 📊 Отчет о прогрессе урока

## Статистика
- Всего вопросов: 15
- Правильных ответов: 12
- Точность: 80%

## Блоки для повторения
- Количество неправильных блоков: 1
- Сгенерировано ремедиалов: 2

### Темы, требующие повторения:
- **Модальные глаголы** (modals) - 1 ошибка
*/
```

## 🎨 Визуальный поток

```
┌─────────────────────────────────────────┐
│   Основные 15 блоков урока              │
│  ✅ ✅ ❌ ✅ ✅ ✅ ❌ ✅ ... ✅         │
└─────────────────────────────────────────┘
              ↓
    ❌ Ошибки обнаружены
              ↓
┌─────────────────────────────────────────┐
│ 🔄 Повторение трудного материала       │
├─────────────────────────────────────────┤
│ Ремедиал #1 (другие примеры)            │
│ Ремедиал #2 (упрощенное объяснение)     │
└─────────────────────────────────────────┘
              ↓
    ✅ Материал закреплен
              ↓
┌─────────────────────────────────────────┐
│ ✅ Отлично! Вы готовы продолжать        │
└─────────────────────────────────────────┘
```

## 🎯 Преимущества системы

✅ **Адаптивное обучение** - ремедиалы генерируются только для ошибок  
✅ **Разнообразие** - новые примеры и формулировки в ремедиалах  
✅ **Подпомощь** - упрощенные объяснения для сложного материала  
✅ **Закрепление** - повторение в конце урока фиксирует знания  
✅ **Прозрачность** - студент видит статистику своих ошибок  

## 💡 Примеры ремедиальных подходов

### Для блока "Модальные глаголы"

**Оригинальный блок:**
> "Must: необходимость (должен). Пример: You must do homework."

**Ремедиал #1 (Подход #2):**
> "Must выражает ОБЯЗАТЕЛЬНОЕ действие. Думайте: это то, что НЕВОЗМОЖНО избежать. Например: You must submit the project by Friday."

**Ремедиал #2 (Подход #3):**
> "Must часто используется в ПРАВИЛАХ и ПРИКАЗАХ. Примеры из жизни: You must wear a seatbelt. Students must complete their homework."

## 🚀 Следующие улучшения

- [ ] Адаптивная сложность ремедиалов
- [ ] Спaced repetition (интервальное повторение)
- [ ] Персонализированные подсказки
- [ ] Интеграция с анализом ошибок студента

