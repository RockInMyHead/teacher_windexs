# 🎤 Голосовое общение в интерактивных уроках

## Что это?

Полноценная система голосового взаимодействия между преподавателем (AI) и учеником:
- **TTS (Text-to-Speech)** - Преподаватель озвучивает материал и ответы
- **STT (Speech-to-Text)** - Система слушает и распознаёт речь ученика
- **Двусторонняя коммуникация** - Естественное общение на русском языке

## ✨ Основные возможности

### 1. Озвучивание контента урока
- Первый экран урока автоматически озвучивается
- Материал разбивается на предложения с 300ms паузами
- Поддержка разных голосов и скоростей

### 2. Слушание ученика
- Кнопка микрофона для начала записи
- Визуальный индикатор "Слушаю вас..."
- Интеримный текст показывается во время распознавания
- Финальный результат автоматически переходит в поле ввода

### 3. Интерактивность
- Ученик может говорить вместо типирования
- Система понимает голосовые команды ("Далее", "Помощь", etc.)
- Естественное взаимодействие с AI преподавателем

### 4. Поддержка браузеров
- Chrome 71+ ✅
- Firefox 95+ ✅
- Safari 14.1+ ✅
- Edge 79+ ✅

## 🚀 Быстрый старт

### Установка

Модули уже установлены. Проверьте наличие файлов:

```bash
ls src/lib/voiceComm.ts           # Основной модуль
ls src/hooks/useVoiceComm.ts      # React хук
ls src/components/InteractiveLessonChat.tsx  # Компонент урока
```

### Использование в компоненте

**Способ 1: Простое использование в InteractiveLessonChat**

Компонент уже настроен с голосовым общением. Просто используйте:

```tsx
import { InteractiveLessonChat } from '@/components/InteractiveLessonChat';

<InteractiveLessonChat
  lessonTitle="Английский: Present Continuous"
  lessonTopic="Настоящее продолжительное время"
  lessonAspects="..."
  lessonContent={{...}}
  onComplete={() => {...}}
/>
```

**Способ 2: Использование хука в своём компоненте**

```tsx
import { useVoiceComm } from '@/hooks/useVoiceComm';

function MyComponent() {
  const {
    isListening,
    isPlaying,
    finalTranscript,
    speak,
    startListening,
    stopListening
  } = useVoiceComm({
    language: 'ru-RU',
    ttsVoice: 'nova',
    ttsSpeed: 1.0
  });

  return (
    <>
      <button onClick={startListening}>
        {isListening ? '🎙️ Слушаю...' : '🎤 Начать'}
      </button>
      <button onClick={() => speak('Привет!')}>
        🔊 Озвучить
      </button>
    </>
  );
}
```

**Способ 3: Прямое использование VoiceComm**

```tsx
import { VoiceComm } from '@/lib/voiceComm';

// Инициализация
VoiceComm.init({
  language: 'ru-RU',
  ttsVoice: 'nova'
}, {
  onTranscript: (text, isFinal) => {
    if (isFinal) console.log('Распознано:', text);
  }
});

// Использование
VoiceComm.startListening();
await VoiceComm.speakText('Привет!');
VoiceComm.cleanup();
```

## 📚 Примеры использования

Смотрите файл `src/examples/VoiceCommExamples.tsx`:

1. **BasicVoiceExample** - Базовое использование
2. **VoiceChatExample** - Чат с голосовым вводом
3. **VoiceSettingsExample** - Настройка параметров
4. **BrowserSupportExample** - Проверка поддержки
5. **ErrorHandlingExample** - Обработка ошибок

Запустите примеры:
```bash
# Импортируйте компонент в свою страницу
import { VoiceCommExamplesPage } from '@/examples/VoiceCommExamples';

// Используйте
<VoiceCommExamplesPage />
```

## 🎯 API Reference

### VoiceComm

```typescript
// Инициализация
VoiceComm.init(options, callbacks) -> boolean

// STT (Слушание)
VoiceComm.startListening() -> boolean
VoiceComm.stopListening() -> void
VoiceComm.abortListening() -> void

// TTS (Озвучивание)
VoiceComm.speakText(text, options) -> Promise<void>
VoiceComm.stopSpeaking() -> void

// Состояние
VoiceComm.getStatus() -> { isListening, isPlaying, ... }
VoiceComm.isAvailable() -> boolean

// Управление
VoiceComm.reset() -> void
VoiceComm.cleanup() -> void
```

### useVoiceComm Hook

```typescript
const {
  // Состояние
  isListening,
  isPlaying,
  interimTranscript,
  finalTranscript,
  isAvailable,
  error,

  // Методы
  startListening,
  stopListening,
  abortListening,
  speak,
  stopSpeaking,
  reset,
  getStatus
} = useVoiceComm(options, onTranscriptCallback);
```

## 🎨 UI Компонента

### Элементы интерфейса

**Кнопка микрофона:**
- Обычная (outline) - готов к записи
- Красная (destructive) - идёт запись
- Отключена - если TTS воспроизводится

**Индикатор записи:**
```
┌─ Слушаю вас... 📳
│ [интеримный текст]
└─
```

**Кнопка громкости:**
- Обычная - готов к озвучиванию
- Активная (primary) - идёт озвучивание
- Отключена - если идёт запись

## ⚙️ Конфигурация

### Переменные окружения

```env
# .env или .env.local
VITE_OPENAI_API_KEY=sk-your-key-here
```

### Параметры инициализации

```typescript
interface VoiceCommOptions {
  language?: string;  // ru-RU, en-US, etc.
  ttsVoice?: 'alloy' | 'echo' | 'fable' | 'onyx' | 'nova' | 'shimmer';
  ttsSpeed?: number;  // 0.25 - 4.0
  continuous?: boolean;  // Продолжать запись после пауз
}
```

### Голоса TTS

| Голос | Тип | Язык | Рекомендация |
|-------|-----|------|------------|
| **nova** | Женский | RU/EN | 👍 Рекомендуется (по умолчанию) |
| **shimmer** | Женский | RU/EN | Для динамичных уроков |
| **alloy** | Мужской | RU/EN | Для технических уроков |
| **echo** | Мужской | RU/EN | Для важной информации |
| **fable** | Мужской | EN | Для английских уроков |
| **onyx** | Мужской | RU/EN | Для мотивирующих уроков |

## 🔍 Отладка

### Логирование в консоли

Все операции логируются в консоль браузера (F12):

```
🎤 Initializing Voice Communication...
✅ Voice Communication initialized
🎙️ Listening started
✅ Final transcript: "Привет, как дела?"
🎤 Starting sentence-by-sentence TTS...
🎵 Speaking sentence 1/3: "Привет!..."
✅ Sentence 1 completed
🔊 TTS started
✅ TTS ended
```

### Проверка поддержки

```typescript
const support = VoiceUtils.checkBrowserSupport();
console.log(support);
// { speechRecognition: true, audioAPI: true, mediaDevices: true }

if (!support.speechRecognition) {
  console.warn('⚠️ Speech Recognition не поддерживается');
}
```

### Проверка разрешений

```typescript
const hasMicPermission = await VoiceUtils.checkMicrophonePermission();
if (!hasMicPermission) {
  console.error('❌ Нет доступа к микрофону');
}
```

## 🛠️ Решение проблем

### Проблема: "Не слышу речь"
- ✅ Говорите громче
- ✅ Проверьте работу микрофона (в системных параметрах)
- ✅ Убедитесь, что браузер имеет доступ к микрофону

### Проблема: "Микрофон недоступен"
- ✅ Проверьте разрешения браузера (Settings > Site Settings > Microphone)
- ✅ Перезагрузите страницу
- ✅ Попробуйте другой браузер

### Проблема: "Озвучивание не работает"
- ✅ Проверьте ключ OpenAI API
- ✅ Проверьте интернет соединение
- ✅ Смотрите ошибки в консоли браузера

### Проблема: "Система зависает"
- ✅ Откройте Developer Tools (F12)
- ✅ Посмотрите консоль на ошибки
- ✅ Перезагрузите страницу (Ctrl+R)
- ✅ Очистите кэш (Ctrl+Shift+Del)

## 📊 Технические детали

### Архитектура

```
┌─ VoiceComm (Main Module)
│  ├─ TTS (OpenAI API)
│  │  └─ Web Audio API
│  │
│  └─ STT (Web Speech API)
│     └─ SpeechRecognition
│
└─ useVoiceComm (React Hook)
   ├─ State Management
   └─ Callbacks
```

### Поток данных

```
User Speech
    ↓
[Web Speech API]
    ↓
Interim Transcript → UI
    ↓
Final Transcript → Input Field
    ↓
[Send to AI]
    ↓
AI Response
    ↓
[OpenAI TTS]
    ↓
[Web Audio API]
    ↓
User Hears Audio
```

## 📖 Полная документация

Смотрите `docs/VOICE_COMMUNICATION_GUIDE.md` для полной документации.

## 🧪 Тестирование

### Ручное тестирование

1. **STT:**
   - Нажать 🎤
   - Произнести текст
   - Проверить распознавание в поле ввода

2. **TTS:**
   - Нажать 🔊
   - Прослушать озвучивание
   - Проверить правильность произношения

3. **Взаимодействие:**
   - Получить ответ от AI
   - Озвучить ответ кнопкой 🔊
   - Во время озвучивания попытаться нажать 🎤 (должна быть заблокирована)

### Автоматизированное тестирование

```bash
# Запустить тесты
npm run test

# Тесты для voiceComm
npm run test -- voiceComm
```

## 🤝 Вклад

При обнаружении ошибок:
1. Откройте Developer Console (F12)
2. Скопируйте логи
3. Создайте Issue с логами и описанием

## 📝 История изменений

### v1.0.0 (Current)
- ✅ Базовая поддержка TTS/STT
- ✅ React хук для управления
- ✅ Интеграция с InteractiveLessonChat
- ✅ Поддержка русского языка
- ✅ Обработка ошибок
- ✅ Примеры использования

## 🚀 Планы развития

- [ ] Поддержка дополнительных языков
- [ ] Оффлайн режим с использованием local TTS
- [ ] Улучшение распознавания акцентов
- [ ] Запись сессий для анализа
- [ ] Интеграция с аналитикой

## 📞 Поддержка

При возникновении вопросов или проблем:
1. Смотрите `docs/VOICE_COMMUNICATION_GUIDE.md`
2. Проверьте `src/examples/VoiceCommExamples.tsx`
3. Посмотрите логи в консоли браузера
4. Создайте Issue с подробным описанием

---

**Автор:** AI Learning System  
**Версия:** 1.0.0  
**Дата:** Ноябрь 2025

Наслаждайтесь голосовым общением! 🎤🎉

