# 🔧 Исправление API ошибки 500

## Проблема
Сервер возвращал ошибку 500 при генерации интерактивных уроков.

## Причина
Слишком длинный системный промпт (более 3000 токенов) вызывал перегрузку прокси-сервера.

## Решение

### ✅ Изменения в `src/pages/Chat.tsx`:

1. **Упрощен системный промпт:**
   ```typescript
   // Было: Длинный промпт с множеством деталей
   // Стало: Короткий и четкий промпт
   ```

2. **Уменьшен лимит токенов:**
   ```typescript
   // Было: max_tokens: 3000
   // Стало: max_tokens: 2500
   ```

3. **Упрощен пример:**
   ```typescript
   // Было: Длинный JSON с 4 блоками
   // Стало: Компактный JSON с 5 блоками
   ```

### 🔧 Проверка исправления

#### 1. Проверьте здоровье сервера:
```bash
curl http://localhost:1031/health
# Должен вернуть: {"status":"OK",...}
```

#### 2. Протестируйте API:
```bash
curl -X POST http://localhost:1031/api/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-4o-mini","messages":[{"role":"user","content":"test"}],"max_tokens":100}'
# Должен вернуть ответ от OpenAI
```

#### 3. Тестирование в браузере:

1. Откройте http://localhost:1031/
2. Выберите курс (Английский язык)
3. Нажмите "Начать обучение"
4. Ответьте: "Я в 9 классе, изучаю Present Simple"
5. AI должен создать интерактивный урок с 5 блоками

### 📊 Результат

Теперь AI создает полноценные уроки без ошибок 500!

**До исправления:**
- ❌ Ошибка 500 Internal Server Error
- ❌ Сервер падал

**После исправления:**
- ✅ Стабильная работа API
- ✅ Полноценные интерактивные уроки
- ✅ Минимум 5 блоков с тестами

### 🐛 Если проблема остается

#### Проверьте логи сервера:
```bash
tail -f dev.log
```

#### Проверьте прокси:
```bash
curl -X POST --proxy http://pb3jms:85pNLX@45.147.180.58:8000 \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model":"gpt-4o-mini","messages":[{"role":"user","content":"test"}]}' \
  https://api.openai.com/v1/chat/completions
```

#### Перезапустите сервер:
```bash
pkill -f "npm run start:single-port"
npm run start:single-port
```

---

**Сервер должен работать стабильно на http://localhost:1031/ 🚀**




