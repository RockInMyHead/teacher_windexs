console.log("Testing API key:", process.env.OPENAI_API_KEY ? "SET" : "NOT SET"); const fetch = require("node-fetch"); async function test() { try { const response = await fetch("https://api.openai.com/v1/models", { headers: { "Authorization": `Bearer ${process.env.OPENAI_API_KEY}` } }); console.log("API test status:", response.status); } catch(e) { console.log("API test error:", e.message); } } test();
